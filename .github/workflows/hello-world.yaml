name: Hello world

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hello
        run: echo "Hello World"

      - name: Initialize
        run: npm install

      - name: Lint
        run: npm run lint